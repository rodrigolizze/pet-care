<div class="container py-3">
  <h1 class="mb-4">Nova Disponibilidade</h1>

  <div class="card shadow-sm">
    <div class="card-body">
      <% if alert.present? %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>

      <p class="text-muted small mb-4">
        Selecione um período. Criaremos uma disponibilidade para <em>cada dia</em> dentro do intervalo.
      </p>

      <%= form_with url: availabilities_path, method: :post, local: true, class: "row g-3" do %>
        <div class="col-12 col-md-6">
          <%= label_tag :start_date, "Data de início", class: "form-label" %>
          <%= date_field_tag :start_date, params[:start_date], min: Date.current, class: "form-control" %>
          <div class="form-text">A partir de hoje.</div>
        </div>

        <div class="col-12 col-md-6">
          <%= label_tag :end_date, "Data de término", class: "form-label" %>
          <%= date_field_tag :end_date, params[:end_date], min: Date.current, class: "form-control" %>
          <div class="form-text">Inclui o dia final.</div>
        </div>

        <div class="col-12 d-flex gap-2 pt-2">
          <%= submit_tag "Criar disponibilidades", class: "btn btn-success" %>
          <%= link_to "Cancelar", availabilities_path, class: "btn btn-outline-secondary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
