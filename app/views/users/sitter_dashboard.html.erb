<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="mb-0">Sitter Dashboard</h1>
    <%= link_to "Adicionar Disponibilidade", new_availability_path, class: "btn btn-success" %>
  </div>

  <div class="alert alert-info">
    Bem-vindo, <strong><%= current_user.name %></strong>! Aqui você pode gerenciar suas disponibilidades e ver informações do seu perfil.
  </div>

  <h3 class="mt-4">Minhas Disponibilidades</h3>
  <% if @availabilities.any? %>
    <table class="table table-hover align-middle">
      <thead class="table-light">
        <tr>
          <th>Data</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% @availabilities.each do |availability| %>
          <tr>
            <td><%= availability.date.strftime("%d/%m/%Y") %></td>
            <td>
              <%= button_to "Excluir",
                    availability_path(availability),
                    method: :delete,
                    data: { confirm: "Tem certeza que deseja excluir esta disponibilidade?" },
                    class: "btn btn-sm btn-outline-danger" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="text-muted">Você ainda não adicionou nenhuma disponibilidade.</p>
  <% end %>

  <hr class="my-4">

  <h3>Sobre Mim</h3>
  <div class="card p-3 shadow-sm">
    <p><strong>Bio:</strong> <%= current_user.bio.presence || "Nenhuma bio fornecida." %></p>
    <p><strong>Experiência:</strong> <%= current_user.experience.presence || "Nenhuma experiência informada." %></p>
  </div>
</div>
